import { IGuide } from "./guide.interface"

const applyForGuide = async (userId: string, payload: Partial<IGuide>) => {
    return
}

const approveGuide = async () => {
    return
}

const getAllGuides = async () => {
    return
}


export const GuideService = {
    applyForGuide,
    approveGuide,
    getAllGuides
}